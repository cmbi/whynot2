### Op CMBI4: ###

	svn checkout https://svn.cmbi.ru.nl/repos/whynot/database/ database/;
	cd database;

Het opnieuw aanmaken van de database whynot en user whynot_owner:

	sh build.db;

### Op CMBI8: ###

	svn checkout https://svn.cmbi.ru.nl/repos/whynot/ whynot/;
	cd whynot;

Het aanpassen van de paden naar de databases:

	vi crawler.sh;

Het aanmaken van de standaard configuratie files:

	sh crawler.sh;
	sh commenter.sh;

Het aanpassen van de standaard configuratie files:

	vi jdbc.xml;
	vi databases.xml;

Het initieel vullen van de database:

	sh crawler.sh
	cp backups/20070808_comments.txt comments/;
	sh commenter.sh;

Het dagelijks (bij voorkeur 's nachts) uit te voeren commando:

	sh run-daily.sh;

Het installeren van de webapplicatie:

	cp whynot.war /usr/tomcat/webapps/whynot.war;

Het configureren van de webapplicatie:

	firefox localhost:8080/manager/html;